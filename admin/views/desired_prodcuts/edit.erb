<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:desired_prodcuts, :index) %></li>
  <li> <%= link_to tag_icon(:plus, pat(:new)), url(:desired_prodcuts, :new) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, pat(:edit)), url(:desired_prodcuts, :edit, :id => @desired_prodcut.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :desired_prodcut, url(:desired_prodcuts, :update, :id => @desired_prodcut.id), :method => :put, :class => 'form-horizontal' do |f| %>
    <%= partial 'desired_prodcuts/form', :locals => { :f => f } %>
  <% end %>
</div>
