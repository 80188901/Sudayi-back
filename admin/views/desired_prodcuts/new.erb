<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:desired_prodcuts, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:desired_prodcuts, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :desired_prodcut, url(:desired_prodcuts, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'desired_prodcuts/form', :locals => { :f => f } %>
 <% end %>
</div>
