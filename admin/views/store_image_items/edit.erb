<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:store_image_items, :index) %></li>
  <li> <%= link_to tag_icon(:plus, pat(:new)), url(:store_image_items, :new) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, pat(:edit)), url(:store_image_items, :edit, :id => @store_image_item.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :store_image_item, url(:store_image_items, :update, :id => @store_image_item.id), :method => :put, :class => 'form-horizontal' do |f| %>
    <%= partial 'store_image_items/form', :locals => { :f => f } %>
  <% end %>
</div>
