<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:wishes, :index) %></li>
  <li> <%= link_to tag_icon(:plus, pat(:new)), url(:wishes, :new) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, pat(:edit)), url(:wishes, :edit, :id => @wish.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :wish, url(:wishes, :update, :id => @wish.id), :method => :put, :class => 'form-horizontal' do |f| %>
    <%= partial 'wishes/form', :locals => { :f => f } %>
  <% end %>
</div>
