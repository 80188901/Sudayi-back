<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:wishes, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:wishes, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :wish, url(:wishes, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'wishes/form', :locals => { :f => f } %>
 <% end %>
</div>
