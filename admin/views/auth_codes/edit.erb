<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:auth_codes, :index) %></li>
  <li> <%= link_to tag_icon(:plus, pat(:new)), url(:auth_codes, :new) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, pat(:edit)), url(:auth_codes, :edit, :id => @auth_code.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :auth_code, url(:auth_codes, :update, :id => @auth_code.id), :method => :put, :class => 'form-horizontal' do |f| %>
    <%= partial 'auth_codes/form', :locals => { :f => f } %>
  <% end %>
</div>
