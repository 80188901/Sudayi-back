<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:auth_codes, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:auth_codes, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :auth_code, url(:auth_codes, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'auth_codes/form', :locals => { :f => f } %>
 <% end %>
</div>
