<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:coments, :index) %></li>
  <li> <%= link_to tag_icon(:plus, pat(:new)), url(:coments, :new) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, pat(:edit)), url(:coments, :edit, :id => @coment.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :coment, url(:coments, :update, :id => @coment.id), :method => :put, :class => 'form-horizontal' do |f| %>
    <%= partial 'coments/form', :locals => { :f => f } %>
  <% end %>
</div>
