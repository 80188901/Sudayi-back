<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:employees, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:employees, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :employee, url(:employees, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'employees/form', :locals => { :f => f } %>
 <% end %>
</div>
