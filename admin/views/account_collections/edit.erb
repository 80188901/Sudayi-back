<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:account_collections, :index) %></li>
  <li> <%= link_to tag_icon(:plus, pat(:new)), url(:account_collections, :new) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, pat(:edit)), url(:account_collections, :edit, :id => @account_collection.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :account_collection, url(:account_collections, :update, :id => @account_collection.id), :method => :put, :class => 'form-horizontal' do |f| %>
    <%= partial 'account_collections/form', :locals => { :f => f } %>
  <% end %>
</div>
